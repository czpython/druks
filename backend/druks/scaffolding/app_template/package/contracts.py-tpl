from druks.agents import AgentOutput

# Every schema sent to or received from an agent — nowhere else. Schemas an agent
# RETURNS subclass ``AgentOutput`` (the platform unwraps it via to_result()); schemas
# sent INTO an agent are plain ``pydantic.BaseModel``.
