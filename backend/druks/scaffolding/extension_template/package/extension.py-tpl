from druks.extensions import Extension


class {{ Name }}(Extension):
    name = "{{ name }}"
    # Any Lucide glyph name the frontend bundles ("telescope", "hammer", ...).
    icon = "box"
    description = "TODO: one-line blurb shown in the settings pane."

    # Operator-tunable knobs go on an inner ``class Settings(ExtensionSettings)``
    # after importing ExtensionSettings from druks.extensions —
    # the platform picks up an inner class named ``Settings`` and turns its fields
    # (default + type + constraints + title/description) into overridable settings.
