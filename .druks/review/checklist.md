Hold every touched file to the DRUKS checklist and craft gate in `AGENTS.md`, end
to end — not just the diff. A pre-existing smell in a file the change touches is in
scope, even on lines the diff leaves alone. Run the checklist's greps against the
touched files as your tells.
